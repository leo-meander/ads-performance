{
  "name": "meander-ads-figma-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Figma plugin that fills MEANDER ad templates from platform render jobs.",
  "scripts": {
    "build": "esbuild code.ts --bundle --target=es2017 --outfile=dist/code.js",
    "watch": "esbuild code.ts --bundle --target=es2017 --outfile=dist/code.js --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
